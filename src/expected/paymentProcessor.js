var total = 0;

module.exports = {
  processPayment: function(amount){ },
  getPayment: function(){ return 0; },
  canAfford: function(amount){ return false; },
  purchase: function(amount){ return false; }
};